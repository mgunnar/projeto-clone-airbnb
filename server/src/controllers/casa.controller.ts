import { Request, Response, NextFunction } from 'express';
//import { Casa } from '../negocio/conversor';
import { ConversaoDTO } from './conversao.dto';

export async function getCasa(req: Request, res: Response, next: NextFunction) {
    
}